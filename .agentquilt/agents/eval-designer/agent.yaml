description: "Eval design specialist. Triggers on changes to agent instruction sources under .agentquilt/agents/ (beyond mechanical rebuilds), to skills, or to eval strategy; on new or renamed agents; or when a reviewer flags a possible semantic shift in compiled output. Designs static and mock-response evals and detects semantic shifts between compiled prompt versions. Read-only; never runs live model calls."
model: balanced
permissions: read-only
