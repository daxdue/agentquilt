description: "Read-only repository analyst for the investigation stage (INV). Use before planning or implementing any standard or high-risk change to produce an evidence-backed Repository Investigation: affected components, canonical-vs-generated status of every file that may be touched, test and doc surface, and constraint checks. Bash is granted for read-only git history commands only."
model: balanced
permissions: read-only
x-claude:
  tools: "Read, Grep, Glob, Bash"
